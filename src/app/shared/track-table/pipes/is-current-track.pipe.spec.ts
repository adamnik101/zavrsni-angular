import { IsCurrentTrackPipe } from './is-current-track.pipe';

describe('IsCurrentTrackPipe', () => {
  it('create an instance', () => {
    const pipe = new IsCurrentTrackPipe();
    expect(pipe).toBeTruthy();
  });
});
