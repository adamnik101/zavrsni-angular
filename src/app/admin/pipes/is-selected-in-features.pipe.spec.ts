import { IsSelectedInFeaturesPipe } from './is-selected-in-features.pipe';

describe('IsSelectedInFeaturesPipe', () => {
  it('create an instance', () => {
    const pipe = new IsSelectedInFeaturesPipe();
    expect(pipe).toBeTruthy();
  });
});
