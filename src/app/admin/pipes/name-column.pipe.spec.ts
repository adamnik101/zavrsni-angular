import { NameColumnPipe } from './name-column.pipe';

describe('NameColumnPipe', () => {
  it('create an instance', () => {
    const pipe = new NameColumnPipe();
    expect(pipe).toBeTruthy();
  });
});
